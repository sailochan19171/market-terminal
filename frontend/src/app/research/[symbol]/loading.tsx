import { PageLoader } from "@/components/PageLoader";

export default function Loading() {
  return <PageLoader label="Loading valuation research" detail="Fair value models, margin of safety and price zones." />;
}
