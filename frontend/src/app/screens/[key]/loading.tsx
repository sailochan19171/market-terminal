import { PageLoader } from "@/components/PageLoader";

export default function Loading() {
  return <PageLoader label="Running screen" detail="Filtering every listed company against the screen." />;
}
